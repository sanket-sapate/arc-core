version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/repository/queries"
    schema: "../../infra/migrations/notification-service"
    gen:
      go:
        package: "db"
        out: "internal/repository/db"
        sql_package: "pgx/v5"
        emit_interface: true
