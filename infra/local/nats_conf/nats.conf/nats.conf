# NATS server configuration for local development
# JetStream enabled with file storage in /data/nats/jetstream
jetstream {
  store_dir: "/data/nats/jetstream"
  max_mem: 256m
  max_file: 2Gi
}

# Monitoring endpoint
http_port: 8222

# Maximum payload size (1MB)
max_payload: 1MB
